/**
 * Inicialização e Setup de Animações
 */

document.addEventListener("DOMContentLoaded", () => {
    // Confirmação de que o GSAP carregou e o DOM está pronto
    console.log("Esqueleto inicializado. GSAP versão:", gsap.version);

    // As animações das dobras serão registradas aqui
});